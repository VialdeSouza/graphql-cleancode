type Query {
  queryByID(id: ID!): Resident
}

type Resident {
  id: ID!
  name: String!
  email: String!
  isUser: Boolean!
  income: Float!
  idHouse: String!
}

type Mutation {
  createResident(
    name: String!
    email: String!
    isUser: Boolean!
    income: Float!
    idHouse: String!
  ): Resident

  updateResident(
    id: ID!
    name: String!
    email: String!
    isUser: Boolean!
    income: Float!
    idHouse: String!
  ): Resident
}
